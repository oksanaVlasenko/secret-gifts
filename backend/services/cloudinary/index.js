const {uploadCloudinary} = require('./cloudinary')
const { deleteOnCloudinary } = require('./cloudinary')
const { uploadCloudinaryMultiple } = require('./cloudinary')

module.exports = {
    uploadCloudinary,
    deleteOnCloudinary,
    uploadCloudinaryMultiple
}